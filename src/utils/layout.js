// Shared centered-container class for the hospital tenant public site —
// mirrors CompanyLandingPage's own CONTAINER constant so both marketing
// surfaces read as the same product on wide screens instead of the tenant
// site stretching its sections edge-to-edge past ~1280px.
export const SITE_CONTAINER = 'mx-auto max-w-7xl px-6 sm:px-8 md:px-12 lg:px-16 xl:px-20'
